import React from "react";
import { API, Storage } from "aws-amplify";

export const UploadImg = () => {
  try {
    const Data = {};
  } catch (error) {
    console.log("Error uploading image: ", error);
  }
  return <div>UploadImg</div>;
};
